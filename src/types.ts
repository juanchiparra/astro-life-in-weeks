export type WeekItem = {
    week: number;
    text: string;
    style: string;
    tooltip?: string;
    image?: string;
};

export type SectionItem = {
    title: string;
    startDate: string;
    endDate: string;
};
